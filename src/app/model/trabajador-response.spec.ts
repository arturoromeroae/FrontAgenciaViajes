import { TrabajadorResponse } from './trabajador-response';

describe('TrabajadorResponse', () => {
  it('should create an instance', () => {
    expect(new TrabajadorResponse()).toBeTruthy();
  });
});
